<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.dao.RoomDao">
    <select id="roomList" resultType="com.library.bean.Room" >
        SELECT room.room_id,location,seat_sum,room.seat_sum-count(*) as seat_available
        FROM record,room,seat
        where record.seat_id=seat.seat_id and seat.room_id=room.room_id
        GROUP BY room.room_id
    </select>

</mapper>